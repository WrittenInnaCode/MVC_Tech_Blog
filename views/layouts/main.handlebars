<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
        integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous" />
    <link rel="stylesheet" href="/css/style.css">
    <title>writteninnacode TechBlog</title>
</head>

<body class="bg-light">
    <div id="page-container">
        <div id="content-wrap">
            <nav class="navbar navbar-expand-lg bg-white border-bottom pb-2 ps-2">
                <div class="container-fluid">
                    <a class="navbar-brand fw-lighter font-monospace fs-3" href="/"><i class="far fa-keyboard pe-3"></i>writteninnacode_TechBlog</a>
                    <div id="navbarNav">
                        <ul class="navbar-nav">
                            <!-- Conditionally render login or logout links -->
                            {{#if logged_in}}
                            <li class="nav-item pe-2">
                               <a class="nav-link fs-6" href="/">Home</a>
                            </li>
                            <li class="nav-item pe-2">
                               <a class="nav-link fs-6" href="/dashboard">Dashboard</a>
                            </li>
                            <li class="nav-item pe-2">
                                <button id="logout" class="btn btn-outline-danger fs-6">Logout <i class="fas fa-sign-out-alt"></i></button>
                            </li>
                            {{else}}
                            <li class="nav-item pe-2">
                                <a class="nav-link fs-6" href="/login">Login <i class="fas fa-sign-in-alt"></i></a>
                            </li>
                            {{/if}}
                        </ul>
                    </div>
                </div>
            </nav>


            <main>
                <!-- Render the sub layout -->
                {{{ body }}}

            </main>

            <!-- Render script for logged in users only -->
            {{#if logged_in}}
            <script src="/js/logout.js"></script>
            {{/if}}

        </div>
        <footer id="footer" class="d-flex justify-content-center">&copy;
            <script>document.write(new Date().getFullYear());</script> Inna Sherstnyova
        </footer>
    </div>


</body>

</html>